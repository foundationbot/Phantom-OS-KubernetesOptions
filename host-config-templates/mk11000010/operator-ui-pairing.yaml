# mk11000010 operator-ui pairing — placeholder.
# This robot was added on the per-host-pairing branch and never
# carried a Kustomize patch. Replace AI_PC_URL with the paired
# AI PC's Tailscale address before copying to /etc/phantomos/.
apiVersion: v1
kind: ConfigMap
metadata:
  name: operator-ui-pairing
  namespace: argus
data:
  AI_PC_URL: http://REPLACE-WITH-AI-PC-TAILSCALE-IP:5000
