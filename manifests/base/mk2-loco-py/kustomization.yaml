apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# mk2-loco-py DaemonSet — MK2 velocity-twist locomotion as the phantom-locomotion
# -style Python node (50 Hz) + in-container web console. Gated on
# foundation.bot/has-mk2-loco-py; in the positronic/locomotion/sonic/omni-wbc/
# wolverine-loco mutual-exclusion group. Ships OBSERVE_ONLY=1 by default — see
# mk2-loco-py.yaml for the full safety posture + the IMU-verification gate.
resources:
  - mk2-loco-py.yaml
  - configmap.yaml
