apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# wolverine-wbc DaemonSet — the omni-wbc whole-body-controller C++ 1 kHz
# inference node, gated on foundation.bot/has-wbc. Mutually exclusive with
# phantom-locomotion, phantom-sonic, and positronic-control (all drive
# /desired; host-config validator enforces). See wolverine-wbc.yaml for the
# full design notes + the Phase-1 OBSERVE-ONLY hardware-test safety posture.
resources:
  - wolverine-wbc.yaml
