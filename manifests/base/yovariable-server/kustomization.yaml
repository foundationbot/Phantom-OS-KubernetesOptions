apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# The phantom namespace is owned by manifests/base/phantom-namespace/.
# Overlays that include this base must also include base/phantom-namespace
# (or already create the namespace by some other means).
resources:
  - yovariable-server.yaml
