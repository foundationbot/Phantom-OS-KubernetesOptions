apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# policy-diagnostics DaemonSet — gated on foundation.bot/has-policy-diagnostics
# (default off). Standalone dma_diagnostic_node from
# foundationbot/policy-diagnostics-tools. See policy-diagnostics.yaml for the
# full design notes.
resources:
  - policy-diagnostics.yaml
