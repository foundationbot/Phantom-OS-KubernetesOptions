apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# ik-mk2 DaemonSet — MK2 upper-body IK shim (teleop → WBC residual base).
# Gated on foundation.bot/has-ik-mk2, so it ships enabled in git but stays
# inert until a host opts in via host-config.yaml > nodeLabels. Lives in the
# positronic namespace (created by ../positronic); does not re-declare it.
# See ik-mk2.yaml for the full design notes.
resources:
  - ik-mk2.yaml
