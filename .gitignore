argocd
.obsidian/
__pycache__/
*.pyc

# Working drafts (plan + research notes). Kept locally per-author; not
# part of the canonical repo. Anything authored under these paths stays
# off the remote.
docs/plans/
docs/internal/plans/
docs/research/
docs/internal/rfcs/0009-rt-isolation-layered-stack.md
.claude/worktrees/

# Local deb-builder output (scripts/build-deb.sh)
/dist/

# Local Python build venv for tools/phantomos-ops (regeneratable via
# `python -m venv`; machine-specific paths — never commit).
tools/phantomos-ops/.build-venv/

# Exported OCI image tarballs. Large binary artifacts delivered via the
# image-bundle sideload path (see docs/internal/test-image-bundle-locally.md),
# not stored in git.
image-archives/
